<%= form_for(allowed) do |f| %>
  <% if allowed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(allowed.errors.count, "error") %> prohibited this allowed from being saved:</h2>

      <ul>
      <% allowed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
