<p id="notice"><%= notice %></p>
<p>
  <strong>Name:</strong>
  <%= @eval.name %>
</p>
<p>
  <strong>Intro:</strong>
  <%= @eval.intro %>
</p>
<p>
  <strong>Nb de questions :</strong>
  <%= @questions.count %>
</p>
<table class="table table-responsive">
  <thead>
    <tr>
      <th>Question</th>
      <th></th>
    </tr>
    <% @questions.each do |quest|%>
      <tbody>
        <tr>
          <td><%= quest.statement.html_safe %></td>
          <td><%= link_to "edit", edit_eval_question_path(@eval,quest), class: "btn btn-xs btn-default"%></td>
        </p>
      <% end %>
    </tr>
  </tbody>
</table>
<br>
<%= link_to 'Edit', edit_eval_path(@eval), class: "btn btn-default" %>
<%= link_to 'add_question', new_eval_question_path(@eval), class: "btn btn-default" %>
<%= link_to 'Back', evals_path , class: "btn btn-default"%>
